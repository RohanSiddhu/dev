# CMakeList.txt : CMake project for InsertionSort.
#
cmake_minimum_required (VERSION 3.8)

# Add source to this project's executable.
add_executable (InsertionSort "InsertionSort.cxx" "InsertionSort.h")
